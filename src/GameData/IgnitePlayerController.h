#pragma once
#include "../Core/PlayerController.h"

class IgnitePlayerController : public PlayerController {
public:
    void HandleInput() override;
    ~IgnitePlayerController() override = default;

protected:

private:

};


